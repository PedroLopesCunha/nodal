Hello <%= @customer.contact_name %>

Your order is confirmed. You ordered:
<% @order.order_items.each do |item| %>
- <%= item.product.name %><% if item.product_variant.present? && item.product.variable? %> (<%= item.product_variant.sku.presence || item.product_variant.option_values_string %>)<% end %> x<%= item.quantity %>
<% end %>
